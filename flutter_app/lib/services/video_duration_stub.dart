import 'package:image_picker/image_picker.dart';

/// Stub: na web não é possível obter duração do vídeo por path. Retorna null (validação ignorada).
Future<int?> getVideoDurationSeconds(XFile? file) async => null;
