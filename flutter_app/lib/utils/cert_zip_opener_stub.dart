import 'dart:typed_data';

/// Web: sem `dart:io` — retorna null (o chamador usa [Share] com bytes em memória).
Future<String?> writeCertZipAndOpen(Uint8List bytes, String filename) async {
  return null;
}
