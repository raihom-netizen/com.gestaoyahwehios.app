import 'package:flutter/material.dart';

/// Stub: não faz nada (web, Android, iOS).
Future<void> initWindowCloseHandler(GlobalKey<NavigatorState> navigatorKey) async {
  // Nenhuma ação em plataformas sem janela nativa
}
