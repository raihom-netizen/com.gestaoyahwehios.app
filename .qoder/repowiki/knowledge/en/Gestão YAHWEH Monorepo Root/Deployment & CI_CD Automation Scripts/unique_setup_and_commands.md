Node scripts require `npm install` in `scripts/` and a Firebase Admin SDK key via `GOOGLE_APPLICATION_CREDENTIALS` pointing to `secrets/gestaoyahweh-21e23-7951f1817911.json`. Full production deploy is triggered from the repo root with `./scripts/deploy_completo.ps1` (supports flags like `-CopyTo`, `-SkipGitPush`, `-ForceFunctions`, `-LogTo`). iOS signing in Codemagic uses environment variables `CM_CERTIFICATE`, `CM_PROVISIONING_PROFILE`, and a mode file `/tmp/cm_yw_signing_mode` selecting manual P12+profile, team_signing, or API-only paths.