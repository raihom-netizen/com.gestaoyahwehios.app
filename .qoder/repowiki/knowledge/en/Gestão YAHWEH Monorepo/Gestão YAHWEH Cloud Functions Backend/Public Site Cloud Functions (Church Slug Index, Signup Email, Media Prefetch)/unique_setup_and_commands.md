Email sending requires the `SENDGRID_API_KEY` and `SENDGRID_FROM_EMAIL` environment variables to be set in the GCP/Firebase project; passing `unset` as either value makes the function log and skip sending instead of failing. The slug backfill endpoint accepts only requests from the hard-coded email `raihom@gmail.com` and must be invoked through the Firebase Functions emulator or deployed callable endpoint.